{
  "name": "sheerdevelopment",
  "license": "MIT",
  "engines": {
    "node": "12.4.0",
    "yarn": "1.16.0"
  },
  "scripts": {
    "build": "yarn --cwd frontend install && yarn --cwd frontend build",
    "deploy": "cp -a frontend/dist/. public/",
    "heroku-postbuild": "yarn build && yarn deploy"
  }
}
